<h2>Edit check</h2>

<%= render "form.html", changeset: @changeset,
                        categories: @categories,
                        action: check_path(@conn, :update, @check) %>

<%= link "Back", to: check_path(@conn, :index) %>
