<h2>New check</h2>

<%= render "form.html", changeset: @changeset,
                        categories: @categories,
                        action: check_path(@conn, :create) %>

<%= link "Back", to: check_path(@conn, :index) %>
